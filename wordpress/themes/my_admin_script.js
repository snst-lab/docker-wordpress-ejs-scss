/*global jQuery,console*/
jQuery(function ($) {
    'use strict';

    //$('#mw_adminimize_login').append('｜<a class="ab-item" href="/_wp/wp-content/themes/original-theme/WP4_Manual3.pdf" target="_blank">マニュアルPDFを見る</a>');

    //カスタム投稿タイプ「news」に対する処理

    /*
    if ($('body').hasClass('post-type-news')) {

        if ($('body').hasClass('edit-php')) {
            $('.subsubsub').before('<div id="messageLoad"></div>');
            $('#messageLoad').load('/_wp/wp-content/themes/original-theme/message/news.html');
        }

        if ($('body').hasClass('post-php') || $('body').hasClass('post-new-php')) {
            console.log();
        }


        if ($('body').hasClass('edit-tags-php')) {
            console.log();
        }
    }
    
    */
});
